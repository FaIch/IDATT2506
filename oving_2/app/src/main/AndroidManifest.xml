<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Oving_2"
        tools:targetApi="31">
        <activity
            android:name=".RandomNumberActivity"
            android:exported="false"
            android:label="@string/title_activity_random_number"
            android:theme="@style/Theme.Oving_2">
            <intent-filter>
                <action android:name="android.intent.action.RANDOM_NUMBER"/>
                <category android:name="android.intent.category.DEFUALT"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".TaskOneActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Oving_2">
            <intent-filter>
                <action android:name="android.intent.action.TaskOne" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TaskTwoActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Oving_2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>